// Object.values : returns only values
const person = {
  name: "Jalish",
  age: 28,
  favouriteBooks: ["Osomapto Attojiboi", "karagare rojnamocha"],
};
Object.values(person);

// Object.entries : returns both key and values
Object.entries(person);
// Async function: seperate videos

// Exponentiation
console.log(Math.pow(2, 3)); //previous
console.log(2 ** 3); //es7

// Trailing Comas
